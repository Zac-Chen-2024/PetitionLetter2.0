# Copy to frontend/.env.local and edit as needed.

# Backend API base URL. Defaults to http://localhost:8000/api when unset,
# which is correct for the standard local dev setup (uvicorn on :8000).
# Set this when the backend lives elsewhere, e.g. behind a reverse proxy:
# VITE_API_BASE=https://petition.example.com/api
